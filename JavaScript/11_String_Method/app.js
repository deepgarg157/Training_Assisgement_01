let names='deepanshu Garg'
let firstLetter = names.slice(0,1)
let upperCase=firstLetter.toUpperCase()
let restName=names.slice(1,names.length)
let completeName=upperCase+restName
// console.log(completeName);

alert('Hello,'+ completeName);